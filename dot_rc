alias rm='rm -i'
alias cp='cp -i'
alias mv='mv -i'

if [ -f /etc/bashrc ] ; then
  . /etc/bashrc
fi

for file in "${HOME}/.sh/rc"/*.sh ; do
  [ -r "${file}" ] && [ -f "${file}" ] && . "${file}"
done
unset file
